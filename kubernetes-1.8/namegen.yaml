kind: Service
apiVersion: v1
metadata:
  name: namegenservice
  labels:
    app: namegen
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: namegenservice
---
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: namegen
spec:
  selector:
    matchLabels:
      app: namegenservice
  template:
    metadata:
      labels:
        app: namegenservice
    spec:
      containers:
      - name: namegen
        image: christofferjerrebo/namegen-scratch
        ports:
        - name: http
          containerPort: 8080
